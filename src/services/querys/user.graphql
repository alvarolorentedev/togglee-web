query($id: String!) {
    user(id:$id) {
        id,
        email,
        isTest,
        projects {
            name,
            owner,
            id,
            isTest,
            url,
            toggles
        }
    }
}